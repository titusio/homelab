apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wiredoor
  namespace: gateway 
spec:
  interval: 30m
  chart:
    spec:
      chart: wiredoor
      version: 1.1.0
      sourceRef:
        kind: HelmRepository
        name: wiredoor-gateway
        namespace: gateway
  values:
    wiredoor:
      existingSecret: wiredoor-config
      ip: 204.168.137.124
