apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: grafana-db-backup
  namespace: monitoring
spec:
  configuration:
    destinationPath: s3://grafana-db-backup/
    endpointURL: http://scarif-depot.object-storage.svc.cluster.local:3900
    s3Credentials:
      accessKeyId:
        name: grafana-db-backup-ssd-s3
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: grafana-db-backup-ssd-s3
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageBucket
metadata:
  name: grafana-db-backup
  namespace: monitoring
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  globalAlias: grafana-db-backup
  quotas:
    maxSize: 5Gi
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageKey
metadata:
  name: grafana-db-backup-key
  namespace: monitoring
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  bucketPermissions:
    - bucketRef:
        name: grafana-db-backup
      read: true
      write: true
  secretTemplate:
    name: grafana-db-backup-ssd-s3
    accessKeyIdKey: ACCESS_KEY_ID
    secretAccessKeyKey: ACCESS_SECRET_KEY
    regionKey: REGION
