apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nas-tls
  namespace: traefik
spec:
  secretName: nas-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - nas.tituslab.dev
    - nas.titushome.dev

