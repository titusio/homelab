apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: networking-tls
  namespace: traefik
spec:
  secretName: networking-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - speed.tituslab.dev
