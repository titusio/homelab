apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: speedtest-tracker-db-backup
  namespace: networking
spec:
  configuration:
    destinationPath: s3://speedtest-tracker-db-backup/
    endpointURL: http://scarif-depot.object-storage.svc.cluster.local:3900
    s3Credentials:
      accessKeyId:
        name: speedtest-tracker-db-backup-ssd-s3
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: speedtest-tracker-db-backup-ssd-s3
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageBucket
metadata:
  name: speedtest-tracker-db-backup
  namespace: networking
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  globalAlias: speedtest-tracker-db-backup
  quotas:
    maxSize: 10Gi
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageKey
metadata:
  name: speedtest-tracker-db-backup-key
  namespace: networking
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  bucketPermissions:
    - bucketRef:
        name: speedtest-tracker-db-backup
      read: true
      write: true
  secretTemplate:
    name: speedtest-tracker-db-backup-ssd-s3
    accessKeyIdKey: ACCESS_KEY_ID
    secretAccessKeyKey: ACCESS_SECRET_KEY
    regionKey: REGION
