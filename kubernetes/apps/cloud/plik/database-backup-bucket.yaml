apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: plik-db-backup
  namespace: cloud
spec:
  configuration:
    destinationPath: s3://plik-db-backup/
    endpointURL: http://scarif-depot.object-storage.svc.cluster.local:3900
    s3Credentials:
      accessKeyId:
        name: plik-db-backup-ssd-s3
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: plik-db-backup-ssd-s3
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageBucket
metadata:
  name: plik-db-backup
  namespace: cloud
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  globalAlias: plik-db-backup
  quotas:
    maxSize: 15Gi
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageKey
metadata:
  name: plik-db-backup-key
  namespace: cloud
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  bucketPermissions:
    - bucketRef:
        name: plik-db-backup
      read: true
      write: true
  secretTemplate:
    name: plik-db-backup-ssd-s3
    accessKeyIdKey: ACCESS_KEY_ID
    secretAccessKeyKey: ACCESS_SECRET_KEY
    regionKey: REGION
