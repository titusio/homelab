apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: karakeep
  namespace: productivity
spec:
  interval: 30m
  chart:
    spec:
      chart: karakeep
      version: 1.2.4
      sourceRef:
        kind: HelmRepository
        name: helmforgedev
        namespace: apps
  values:
    karakeep:
      nextAuthUrl: "https://karakeep.tituslab.dev"
