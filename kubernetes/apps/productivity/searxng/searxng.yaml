apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: searxng
  namespace: productivity
spec:
  interval: 5m
  chart:
    spec:
      chart: searxng
      version: 1.1.1
      sourceRef:
        kind: HelmRepository
        name: kubitodev
        namespace: productivity
  values:
    replicaCount: 3
