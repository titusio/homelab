apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: paperless-cache
  namespace: productivity
spec:
  interval: 30m
  chart:
    spec:
      chart: valkey
      version: 0.9.4
      sourceRef:
        kind: HelmRepository
        name: valkey
        namespace: valkey
  values: 
    auth:
      enabled: true
      usersExistingSecret: "paperless-cache-credentials"
      aclUsers:
        default:
          permissions: "~* &* +@all"
        readonly:
          permissions: "~* -@all +@read +ping +info"
          passwordKey: "password"  
