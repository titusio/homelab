apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-tls
  namespace: traefik
spec:
  secretName: mealie-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - mealie.tituslab.dev
    - mealie.tituscloud.dev
