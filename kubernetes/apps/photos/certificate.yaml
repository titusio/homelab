apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: photos-tls
  namespace: traefik
spec:
  secretName: photos-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - photos.tituslab.dev
