apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: forgejo-valkey
  namespace: source-control
spec:
  interval: 5m
  chart:
    spec:
      chart: valkey
      version: 0.9.4
      sourceRef:
        kind: HelmRepository
        name: valkey
        namespace: valkey
  values:
    auth:
      enabled: true
      usersExistingSecret: "forgejo-valkey-credentials"
      aclUsers:
        default:
          permissions: "~* &* +@all"
          passwordKey: default-password
