apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: git-tls
  namespace: traefik
spec:
  secretName: git-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - git.titusio.net
