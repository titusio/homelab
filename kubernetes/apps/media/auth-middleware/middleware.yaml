apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: media-oidc-auth
  namespace: media
spec:
  plugin:
    traefik-oidc-auth:
      Secret: "urn:k8s:secret:media-oidc-auth:pluginSecret"
      Provider:
        Url: "urn:k8s:secret:media-oidc-auth:providerUrl"
        ClientId: "urn:k8s:secret:media-oidc-auth:clientId"
        ClientSecret: "urn:k8s:secret:media-oidc-auth:clientSecret"
      Scopes: ["openid", "profile", "email"]
      Authorization:
        AssertClaims:
          - Name: groups
            AnyOf: ["media-admin"]
