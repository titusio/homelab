apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seerr
  namespace: media
spec:
  interval: 10m
  chart:
    spec:
      chart: seer-chart
      version: 3.7.0
      sourceRef:
        kind: HelmRepository
        name: seerr
        namespace: media

