apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: seerr-db-backup
  namespace: media
spec:
  configuration:
    destinationPath: s3://seerr-db-backup/
    endpointURL: http://scarif-depot.object-storage.svc.cluster.local:3900
    s3Credentials:
      accessKeyId:
        name: seerr-db-backup-ssd-s3
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: seerr-db-backup-ssd-s3
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageBucket
metadata:
  name: seerr-db-backup
  namespace: media
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  globalAlias: seerr-db-backup
  quotas:
    maxSize: 5Gi
---
apiVersion: garage.rajsingh.info/v1beta1
kind: GarageKey
metadata:
  name: seerr-db-backup-key
  namespace: media
spec:
  clusterRef:
    name: scarif-depot
    namespace: object-storage
  bucketPermissions:
    - bucketRef:
        name: seerr-db-backup
      read: true
      write: true
  secretTemplate:
    name: seerr-db-backup-ssd-s3
    accessKeyIdKey: ACCESS_KEY_ID
    secretAccessKeyKey: ACCESS_SECRET_KEY
    regionKey: REGION
