apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sabnzbd
  namespace: media
  annotations:
    external-dns.alpha.kubernetes.io/controller: "ignore"
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - sabnzbd.tituslab.dev
  rules:
    - matches:
        - path:
            type: Exact
            value: /ping
      backendRefs:
        - name: sabnzbd
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: media-oidc-auth
      backendRefs:
        - name: sabnzbd
          port: 8080
